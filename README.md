# API Ranking - FastAPI

API REST para ranking de jogadores usando:

- FastAPI
- PostgreSQL

## Executar

pip install -r requirements.txt
uvicorn main:app --reload

## Endpoints

POST /score
GET /ranking
